# directory where from the installator is started
installation_file_local: "/home/vbox/horejsp/yak/standby-v11.6.0.tar.gz"
os_user: oracle
# Variables for batch install of multiplatform
# Agent variables
install_dir: "/usr/dbvisit"
download_directory: "{{install_dir}}/download"
default_backup_dir: "{{install_dir}}/standbymp/backup"
webserver_address: "{{groups.all[-1]}}"
nats_port: 5533
agent_listen_port: 7890
passphrase: "admin"


# Control Center Variables
webserver_port: 4433

# common variables
dbvisit_binary_prefix_agent: "install-agent-"
dbvisit_binary_prefix_console: "install-control-"
